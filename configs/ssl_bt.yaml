seed: 42

device: cuda
image_size: 256
batch_size: 32
epochs: 90
learning_rate: 0.001
weight_decay: 0.0001
lambda_coeff: 0.001

num_workers: 4
pin_memory: true

unlabeled_dir: data/raw/plantvillage
checkpoint_dir: artifacts/ssl_bt
metrics_dir: artifacts/ssl_bt_metrics

lambda_sweep:
  - 0.005
  - 0.001
  - 0.0001